import { UnsaveDirective } from './unsave.directive';

describe('UnsaveDirective', () => {
  it('should create an instance', () => {
    const directive = new UnsaveDirective();
    expect(directive).toBeTruthy();
  });
});
